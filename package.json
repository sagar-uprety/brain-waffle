{
  "scripts": {
    "dev": "snowpack dev",
    "build": "snowpack build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "snowpack": "^3.8.8",
    "tailwindcss": "^3.0.18"
  },
  "dependencies": {
    "three": "^0.132.2"
  }
}
